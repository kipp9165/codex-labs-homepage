services:
  - type: static_site
    name: codex-labs-site
    env: static
    publishDir: public
    buildCommand: "node scripts/generate-checkout-urls-from-csv.cjs"

  - type: web
    name: codex-labs-webhook
    env: node
    rootDir: webhook
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: STRIPE_SECRET_KEY
        sync: false
      - key: STRIPE_WEBHOOK_SECRET
        sync: false
