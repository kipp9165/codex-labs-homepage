services:
  - type: web
    name: codex-labs-webhook
    env: node
    plan: starter
    buildCommand: "npm install"
    startCommand: "node webhook/index.js"
    envVars:
      - key: STRIPE_SECRET_KEY
        sync: false
      - key: STRIPE_WEBHOOK_SECRET
        sync: false
    autoDeploy: true
