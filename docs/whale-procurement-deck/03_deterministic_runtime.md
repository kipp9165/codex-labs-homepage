# Deterministic Runtime

This asynchronous document describes deterministic runtime guarantees for regulated clients.

It explains how equivalent inputs produce equivalent governed outputs under continuity controls.
